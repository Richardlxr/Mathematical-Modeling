
---
## 一、 什么是线性规划？

**数学规划**是运筹学的一个分支，研究在特定**约束条件**下，如何寻找最优的**衡量指标**（目标函数）。
当目标函数和约束条件均为**线性表达式**时，我们称之为**线性规划**。

---

## 二、 线性规划的三要素

### 1. 案例背景
张麻子伙计共 6 人，子弹 1200 发。
- **攻碉楼**：每人贡献 40 点士气，消耗 240 发子弹。
- **追替身**：每人贡献 30 点士气，消耗 120 发子弹。
- **要求**：两件事都得派人（至少 1 人）。
- **目标**：如何分配人数使总士气最大？

### 2. 模型构建
| 要素 | 描述 | 在案例中的体现 |
| :--- | :--- | :--- |
| **决策变量** | 需要决定的未知量 | 设 $x_1$ 为攻碉楼人数，$x_2$ 为追替身人数 |
| **目标函数** | 想要最大化或最小化的指标 | $\max y = 40x_1 + 30x_2$ |
| **约束条件** | 必须遵守的限制规则 | 人数限制、子弹限制、最少人数限制 |

### 3. 数学表达
$$
\begin{aligned}
& \max y = 40x_1 + 30x_2 \\
& \text{s.t. } \begin{cases} x_1 + x_2 \le 6 & \text{(总人数限制)} \\ 240x_1 + 120x_2 \le 1200 & \text{(子弹限制)} \\ x_1 \ge 1, x_2 \ge 1 & \text{(至少派1人)} \end{cases}
\end{aligned}
$$
## （手算的话）拉格朗日乘数法 (Lagrange Multipliers)

### 1. 核心思想
将一个有约束的多元函数问题，通过引入一个“拉格朗日乘子” $\lambda$，转化为一个无约束的方程组求解问题。

### 2. 标准步骤
假设我们要最小化/最大化目标函数 $f(x, y)$，且受到约束条件 $g(x, y) = c$ 的限制。

| 步骤 | 操作内容 | 数学表达 |
| :--- | :--- | :--- |
| **Step 1** | **构造拉格朗日函数** | $F(x, y, \lambda) = f(x, y) + \lambda(g(x, y) - c)$ |
| **Step 2** | **求偏导数** | 分别对 $x, y, \lambda$ 求偏导，并令其为 $0$ |
| **Step 3** | **解方程组** | $\begin{cases} \frac{\partial F}{\partial x} = 0 \\ \frac{\partial F}{\partial y} = 0 \\ \frac{\partial F}{\partial \lambda} = 0 \end{cases}$ |
| **Step 4** | **代入求值** | 将求得的驻点代入原目标函数 $f(x, y)$ 得到最值 |

---

## 三、 MATLAB 标准型与求解

在 MATLAB 中，线性规划的标准型要求目标函数是**最小化**。**如果是求最大化，需要加负号转换。**

### 1. MATLAB 标准型公式
$$
\min_x f^T x \quad \text{s.t.} \quad 
\begin{cases} 
A \cdot x \le b \\ 
Aeq \cdot x = beq \\ 
lb \le x \le ub 
\end{cases}
$$

### 2. 求解函数：`linprog`

```matlab
[x, val] = linprog(f, A, b, Aeq, beq, lb, ub)
```

**参数说明表：**

| 参数 | 含义 | 备注 |
| :--- | :--- | :--- |
| `f` | 目标函数系数列向量 | 若原题是 max，则 `f` 需取反 |
| `A, b` | 不等式约束的系数矩阵和常数向量 | 必须是 $Ax \le b$ 形式 |
| `Aeq, beq` | 等式约束的系数矩阵和常数向量 | $Aeq \cdot x = beq$ |
| `lb, ub` | 变量的下界和上界 | 如 $x \ge 0$ 则 $lb=0$ |
| `x` | 最优解时的决策变量取值 | 返回的具体分配方案 |
| `val` | 目标函数的最小值 | 最终计算出的最优指标 |

---

## 四、 实战案例：投资组合优化

### 1. 问题描述
市场上有 $n$ 种资产 $s_i$ 可供投资。已知各资产的：
- 平均收益率：$r_i$
- 风险损失率：$q_i$
- 交易费率：$p_i$

**目标**：设计投资方案，使净收益尽可能大，总体风险尽可能小。

### 2. 多目标转化策略
在实际建模中，我们通常无法同时让“收益最大”和“风险最小”。常用的处理方法是：**固定一个，优化另一个。**

**转化思路：** 设定一个投资者能接受的最大风险水平 $a$，在风险 $\le a$ 的前提下，追求收益最大化。

### 3. 模型建立
设投资第 $i$ 种资产的资金比例为 $x_i$，总资金为 $M$。
- **目标函数（净收益最大化）**：
  $\max \sum_{i=0}^{n} (r_i - p_i)x_i$
- **约束条件**：
  1. 风险限制：$\frac{q_i x_i}{M} \le a, \quad i=1,2,\dots,n$
  2. 资金归一（总和为M）：$\sum_{i=0}^{n} (1+p_i)x_i = M$
  3. 非负限制：$x_i \ge 0$

---

## 五、 建模避坑指南 💡

1.  **符号转换**：MATLAB 的 `linprog` 默认求 $\min$。如果题目要求 $\max z = 2x_1 + 3x_2$，在代码中应写成 $\min w = -2x_1 - 3x_2$。
2.  **不等式方向**：所有不等式必须统一化为 $\le$。如果题目中是 $x_1 + x_2 \ge 10$，应转化为 $-x_1 - x_2 \le -10$。
3.  **空约束处理**：如果模型中没有等式约束或上下界，在 `linprog` 对应位置输入空矩阵 `[]`。
4.  **解的存在性**：并非所有线性规划都有解，可能会出现：
    *   **无解**：约束条件互相冲突（如 $x>10$ 且 $x<5$）。
    *   **无界解**：目标函数可以无限大。
    *   **无穷多解**：最优解在一条线段上。

---

## 六、 课后练习
**作业 1**：请尝试在 MATLAB 中编写代码，求解第二章中的“张麻子攻碉楼”问题。
*提示：注意将 $\max y$ 转换为 $\min -y$，并正确构建 $A$ 和 $b$ 矩阵。*

---
**更多资源**：关注微信公众号“大师兄的知识库”，回复“数学建模”获取课件与代码。